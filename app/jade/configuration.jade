.step.configuration
  .wrapper
    .option.picked
      .icon(data-id="redundant")
        img.shadow-icon(data-src="#{redundantIcon}")
        .check-icon
          img.shadow-icon(data-src="check")
      .title=redundantTitle
      .blurb=redundantBlurb

    .option
      .icon(data-id="bunkhouse")
        img.shadow-icon(data-src="#{singleIcon}")
        .check-icon
          img.shadow-icon(data-src="check")    
      .title=singleTitle
      .blurb=singleBlurb
      if bunkHouses.length > 1
        .radios.inactive
          ul
            li
              input(type="radio" name="bunkhaus" value="new" checked="checked") 
              .txt Split Onto New Server
            li  
              input(type="radio" name="bunkhaus" value="existing") 
              .txt Existing Host
              select#bunkhaus-picker
                each haus in bunkHouses
                  if haus.current
                    option(value="#{haus.id}" disabled) #{haus.name} (Current)
                  else
                    option(value="#{haus.id}")=haus.name
        
